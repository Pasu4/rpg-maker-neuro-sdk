{
  "name": "rpg-maker-neuro-sdk-proxy-server",
  "private": true,
  "version": "1.0.0",
  "description": "Proxy server for communicating between RPG Maker and the Neuro API",
  "license": "MIT",
  "author": "Pasu4",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --platform=node --bundle --outfile=out/index.js --sourcemap --format=esm",
    "watch": "esbuild src/index.ts --platform=node --bundle --outfile=out/index.js --sourcemap --format=esm --watch",
    "start": "node out/index.js"
  },
  "dependencies": {
    "neuro-game-sdk": "^1.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "^26.0.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "tsc": "^2.0.4",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  }
}
