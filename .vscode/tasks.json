{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Watch (esbuild)",
            "type": "npm",
            "script": "watch",
            "problemMatcher": "$esbuild-watch",
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}/proxy-server"
            }
        }
    ]
}