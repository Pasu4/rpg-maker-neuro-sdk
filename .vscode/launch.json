{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Proxy Server",
            "type": "node",
            "program": "${workspaceFolder}/proxy-server/out/index.js",
            "request": "launch",
            "cwd": "${workspaceFolder}/proxy-server",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "preLaunchTask": "Watch (esbuild)"
        }
    ]
}