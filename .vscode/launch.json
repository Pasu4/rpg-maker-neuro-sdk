{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Proxy Server",
            "type": "node",
            "program": "${workspaceFolder}/proxy-server.js",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}